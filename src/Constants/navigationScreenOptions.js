import { Colors } from './Colors';

/** White app chrome + dark status bar icons (iOS & Android). */
export const defaultScreenOptions = {
  headerShown: false,
  statusBarStyle: 'dark',
  statusBarBackgroundColor: Colors.white,
  contentStyle: { backgroundColor: Colors.white },
};
